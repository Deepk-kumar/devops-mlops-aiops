# Default: isi machine par k3s install hoga (Linux / WSL2 Ubuntu)
[k3s]
localhost ansible_connection=local

# Agar alag VM/server par chalana ho to upar wali line hata do aur ye use karo:
# [k3s]
# k3s-node ansible_host=192.168.1.50 ansible_user=ubuntu
