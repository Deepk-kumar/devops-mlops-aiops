{{- if .Values.monitoring.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: churn-api-dashboard
  labels:
    grafana_dashboard: "1"
data:
  churn-api.json: |-
{{ .Files.Get "dashboards/churn-api.json" | indent 4 }}
{{- end }}
